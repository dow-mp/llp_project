const siteData = require('../data/siteData');

module.exports = {
    // get all parks in need of help
    help_all:,

    // update/edit a single submission from a user
    help_update_put:,

    // delete a submission from a user
    help_delete:,

    // get all volunteer listings
    volunteer_all:,

    // update/edit a single submission from a user
    volunteer_update_put:,

    // delete a submission from a user
    volunteer_delete:,
}